# Personal website

Currently under maintenance, building a home rolled static site builder.

## Building the site

Run `main.py`.

## Code
Example usage for a code block:

```html
<pre><code class="language-js">
const greet = (name) => console.log(`Hello, ${name}!`);
greet("World");
</code></pre>
```

